
News Portal Recommender (Big Data Ready)

Контент-базовый рекоммендер для новостного портала. Работает на TF-IDF + косинусном сходстве, поддерживает ограничение объёма входных данных и длины текстов через переменные окружения, учитывает тематику и «свежесть» новости.

🎯 Что делает

Загружает новости из CSV (без БД) батчем NROWS.

Чистит/нормализует поля, отрезает длинные тексты до MAX_TEXT.

Строит TF-IDF-матрицу размером NROWS × TFIDF_FEATS.

Поиск: полнотекстовый (contains) → если пусто, backoff на семантический (TF-IDF).

Рекомендации: k nearest по косинусу + лёгкая доменная поправка:

бонус за совпадение topic,

временной decay (новости ближе к «сейчас» получают больший вес).

Аналитика: топ тем, таймлайн по месяцам, топ-теги.

🧹 Нормализация/санитайзинг

date переводится в date_dt (устойчивый парсер, обрезает время, поддерживает dayfirst/ISO).

topic_key — нижний регистр + [a-z0-9-] (для ключей/картинок); незнакомые темы → default.

🚀 Запуск локально
python -m venv .venv
source .venv/bin/activate          # Windows: .venv\\Scripts\\activate
pip install -r requirements.txt

# положи CSV в data/news.csv (в git не коммитим)
python app.py                      # если веб-приложение читает этот класс
